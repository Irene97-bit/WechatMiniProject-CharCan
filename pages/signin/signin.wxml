<!--pages/signin/signin.wxml-->
<view class="topbar">
<image class="topbar-image" src="/images/bg4.png"></image>
<view class="slogan">Sign up for CharCan</view>
</view>

<!--<view class="page-section">
  <view class="weui-cells__title">username</view>
    <view class="weui-cells weui-cells_after-title">
      <view class="weui-cell weui-cell_input">
        <input class="weui-input" auto-focus placeholder="username"/>
      </view>
    </view>
    <view class="weui-cells__title">password</view>
    <view class="weui-cells weui-cells_after-title">
      <view class="weui-cell weui-cell_input">
        <input class="weui-input" password type="text" placeholder="8+ characters" />
      </view>
    </view>
  </view>-->
<mp-toptips msg="{{error}}" type="error" show="{{error}}"></mp-toptips>
<view class="page">
    <view class="page__bd">
        <mp-form id="form" rules="{{rules}}" models="{{formData}}">
            <!--<mp-cells title="单选列表项">
                <mp-checkbox-group prop="radio" multi="{{false}}" bindchange="radioChange">
                    <mp-checkbox wx:for="{{radioItems}}" wx:key="value" label="{{item.name}}" value="{{item.value}}" checked="{{item.checked}}"></mp-checkbox>
                </mp-checkbox-group>
            </mp-cells>
            <mp-cells title="复选列表项">
                <mp-checkbox-group prop="checkbox" multi="{{true}}" bindchange="checkboxChange">
                    <mp-checkbox wx:for="{{checkboxItems}}" wx:key="value" label="{{item.name}}" value="{{item.value}}" checked="{{item.checked}}"></mp-checkbox>
                </mp-checkbox-group>
            </mp-cells>-->

            <mp-cells title="" footer="">
                <mp-cell show-error prop="username" title="username" ext-class="">
                    <input bindinput="formInputChange" data-field="un" class="weui-input" placeholder=""/>
                </mp-cell>
                <mp-cell show-error prop="password" title="password" ext-class="">
                    <input bindinput="formInputChange" data-field="pw" class="weui-input" placeholder="8+characters"/>
                </mp-cell>
                <!--<mp-cell prop="mobile" title="tel" ext-class=" weui-cell_vcode">
                    <input bindinput="formInputChange" data-field="mobile" class="weui-input" placeholder="请输入手机号"/>
                    <view slot="footer" class="weui-vcode-btn">获取验证码</view>
                </mp-cell>-->
                </mp-cells>
        </mp-form>

            <view class="weui-cell weui-cell_select">
                <view class="weui-cell__hd weui-cell__hd_in-select-after">
                    <view class="weui-label">selected level</view>
                </view>
                <view class="weui-cell__bd">
                    <picker bindchange="bindLevelChange" value="{{levelsIndex}}" range="{{levels}}">
                        <view class="weui-select weui-select_in-select-after">{{levels[levelIndex]}}</view>
                    </picker>
                </view>
            </view>
        </view>



    
</view>
<view class="signup">
<button bindchange="submitForm">create account</button>
</view>


